const { sendJson } = require('./_lib/http'); const { bootstrapIfNeeded, listSignals } = require('./_lib/service'); module.exports = async (req, res) => { await bootstrapIfNeeded(); sendJson(res, 200, await listSignals()); };
